name: Deploy

on:
  push:
    # branches:
    #   - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy
    steps:
      - uses: actions/checkout@master
      - name: Publish
        uses: alank976/wrangler-action@1.6.0
        with:
          apiToken: ${{ secrets.WRANGLER_API_TOKEN }}
          environment: 'prd'