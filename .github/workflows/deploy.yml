name: Deploy

on:
  push:
    # branches:
    #   - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy
    steps:
      - uses: actions/checkout@master
      - name: Publish
        uses: alank976/wrangler-action@6e2b5eb
        with:
          apiToken: ${{ secrets.WRANGLER_API_TOKEN }}
          environment: 'prd'